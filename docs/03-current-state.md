# Текущее состояние

Дата снимка: **5 августа 2026 года**.

Этот документ описывает фактическое состояние по текущему репозиторию. Он не утверждает, что каждый сценарий уже проверен на реальном Telegram-клиенте и под нагрузкой.

## Техническая основа

- React 19 и TypeScript для Mini App.
- Vite для клиентской сборки.
- Express и единый `server.ts` для API, Telegram long polling, маршрута обработки Telegram updates и серверной логики.
- Отдельная Google-таблица как постоянное production-хранилище; процесс держит очищенное рабочее состояние в памяти.
- Сборка сервера через esbuild.
- Команды `npm run lint`, `npm run build` и `npm run verify:core`.

## Интеграция Google Sheets

Подготовлен двусторонний контур синхронизации слотов доступности:

- серверная авторизация через service account, ключ хранится вне Git;
- запись подтверждённых изменений Mini App в Google Sheets;
- подписанный webhook устанавливаемого Apps Script для изменений из таблицы;
- резервная сверка с таблицей раз в минуту;
- динамическое сопоставление строк по нормализованному имени и постоянному Telegram ID пользователя;
- административная настройка горизонта Mini App от двух до пяти недель и уведомление команды; Google Sheets при этом использует только текущую неделю на листе `ОСНОВА`;
- очистка истёкших недель в понедельник 00:00 по Москве без архива;
- администратор задаёт активные дни недели и диапазон часов; по умолчанию используются понедельник–пятница, 17:00–23:00;
- назначенные собрания создаются и обновляются в командном Google Calendar; отменённые удаляются, а повторная синхронизация не создаёт дубликаты;
- идемпотентное создание отдельного пустого листа-шаблона с сохранением оформления.
- безопасная ротация `ОСНОВА`: резервная копия до изменений, обновление дат, очистка только слот-значений и восстановление чекбоксов; отдельные недельные листы больше не создаются.

Интеграция включена в production. Сервисный аккаунт имеет права редактора, а доступность синхронизируется с отдельной рабочей таблицей `ОСНОВА`. Подробности: [интеграция Google Sheets](./11-google-sheets-integration.md).

## Мероприятия и задачи

- В данных есть несколько одновременно активных мероприятий.
- Каждая обычная или факультетская задача может быть привязана к мероприятию.
- Администратор добавляет, редактирует, завершает и возвращает мероприятия в работу в отдельном разделе админской панели; завершение не удаляет задачи.
- Вверху страницы задач выбирается ровно одно активное мероприятие. Этот выбор одновременно фильтрует мои, открытые, выполненные задачи и бэклог, а новая задача автоматически получает выбранное мероприятие.
- Одна задача может относиться к нескольким блокам исполнителей; поиск и массовое назначение работают по объединению людей выбранных блоков.
- В форме назначения исполнители ищутся по имени, Telegram username и компетенции.
- Длительность собрания ограничена диапазоном от 30 минут до 6 часов.

## Продакшен-развёртывание

На момент снимка проект уже работает не только локально, но и в интернете:

- домен: `https://megaorgiabot.ru`;
- Telegram-бот: `@megaorgi_bot`;
- сервер: Selectel VDS в Санкт-Петербурге, Ubuntu 24.04 LTS;
- публичный IP: `139.100.235.189`;
- путь проекта на сервере: `/opt/megabot`;
- Node-приложение слушает порт `3000`;
- Caddy принимает HTTPS-запросы на `80/443` и проксирует их в `127.0.0.1:3000`;
- PM2 держит процесс `megabot` запущенным и восстанавливает его после перезагрузки сервера;
- Telegram Mini App menu button настроен на `https://megaorgiabot.ru`.

Фактическая production-БД — отдельная Google-таблица со схемой, полным снимком, ревизией и аудитом. Локальный `database.json` больше не используется production-процессом; резервные снимки создаются из Google Sheets в `/opt/megabot/backups`.

## Реализованные роли

- `admin`;
- `organizer`;
- `faculty_responsible`;
- `faculty_helper`.

Целевая роль ответственного за мероприятие или блок пока не выражена отдельной универсальной моделью области доступа.

## Mini App

Реализованы разделы:

- профиль;
- слоты доступности;
- встречи;
- задачи;
- команда;
- факультеты / МФ.

В текущей модели доступны управление участниками и компетенциями, предложения времени встреч, создание и изменение задач, экспорт и факультетские сценарии.

## Telegram-бот

Реализованы:

- личное меню и динамические inline-панели;
- просмотр и сохранение слотов;
- просмотр и создание встреч;
- просмотр, взятие, возврат, создание и завершение задач;
- запрос затраченного времени при завершении задачи;
- отдельное компактное меню факультетских ролей;
- уведомления о задачах, встречах и изменениях статуса;
- командный чат с двухшаговым `/all`, динамическими `/all_<блок>`, `/meeting`, `/deadlines`, `/slots`, `/birthdays`, `/checkin`, `/bindteamchat`, `/cancel` и `/help`;
- напоминания по задачам;
- поздравительные уведомления при настроенных реквизитах.

Подробные существующие сценарии описаны в корневом `BOT_CHAT_SCENARIOS.md`.

## Текущая модель данных

Основные сущности: пользователь, доступность, встреча, задача, напоминание, факультет, сообщение бота и настройки. Задача поддерживает нескольких исполнителей и несколько блоков, приоритет, статусы, время выполнения, факультет и напоминания. Пользователь может загрузить и обрезать аватар; сжатое изображение хранится в основной Google Sheets базе.

## Известные продуктовые проблемы

### Собрания

- Поля создания требуют проверки и исправления.
- Разница между общим, тематическим и персональным собранием недостаточно очевидна пользователю.
- Нет подтверждённого целевого сценария самостоятельного присоединения к открытому собранию.
- Нет полноценного RSVP-состояния «Я приду» и списка ответов.

### Отмечаемость

- Каждая показанная неделя сохраняется отдельной кнопкой; вторую и последующие недели можно сохранить даже при пустой первой.
- Невыбранные часы внутри сохраняемой недели трактуются как недоступные; пустую неделю можно сохранить как явную очистку.
- Подбор собрания должен учитывать обязательных конкретных людей, а не только число доступных участников.
- `ОСНОВА` синхронизируется в обе стороны: webhook применяется сразу, fallback работает каждые 15 секунд, а открытие приложения инициирует свежий импорт.

### Задачи

- Пользователь наблюдал большую задержку при взятии задачи; источник задержки не измерен.
- При завершении фиксируются время и комментарий; исполнитель может исправить их или вернуть свою задачу в работу, администратор имеет доступ ко всем задачам.
- Нужна ясная политика архива вместо ручной очистки рабочего лога.
- Опубликованную задачу редактируют её автор или администратор; отдельная покадровая история каждого изменения пока не ведётся.
- Принудительное назначение частично поддержано логикой создания, но целевой UX и права ответственных необходимо формализовать.
- Нет общего раскрывающегося статуса по каждому исполнителю с двусторонними комментариями.
- Напоминания существуют, но сценарий их создания вместе с ТЗ и быстрая кнопка массового уведомления требуют доработки.

### Интерфейс чата

- Основной принцип динамического сообщения уже используется.
- Общий опыт остаётся фрагментированным и требует проверки на реальных сценариях.
- В качестве продуктового референса указан `https://t.me/ChatGPT5_Midjourney_bot`; изучать нужно механику динамической карточки и быстрых кнопок, а не копировать визуальный стиль.

## Технические риски

- Монолитный `server.ts` усложняет тестирование и независимое развитие доменов.
- Полноснимочная запись в Google Sheets ограничивает горизонтальное масштабирование одним процессом и требует контроля квот API.
- Сервер работает одним PM2-процессом из production-сборки `dist/server.cjs`; горизонтальный запуск нескольких экземпляров пока не поддержан.
- Отсутствие универсального журнала событий блокирует качественную аналитику.
- Не зафиксированы SLO по задержке и доставка/повтор уведомлений.
- Двусторонняя интеграция с таблицами потребует идентификаторов, версий записей и разрешения конфликтов.

## Что считать подтверждённым только после проверки

- корректность всех прав для каждой роли;
- стабильность webhook и уведомлений в реальном окружении;
- производительность операций с задачами;
- визуальная корректность на разных Telegram-клиентах;
- полнота `verify:core` относительно реальных пользовательских путей.

## Подтверждённое состояние на 2026-08-03

- production health endpoint отвечает и сообщает настроенные Telegram, Mini App и Google Sheets;
- адрес Mini App — `https://megaorgiabot.ru`; известные tunnel-домены блокируются в production;
- Telegram UI настраивается при старте и в webhook-режиме, даже если long polling отключён;
- выбор слотов выполняется в одном редактируемом сообщении;
- доступна массовая отметка/очистка дня, обзор показывает динамический счётчик `N/M`;
- черновик хранится рядом с состоянием чатовых панелей и переживает рестарт;
- сохранение обновляет рабочее состояние в памяти и ставит согласованный снимок основной Google Sheets базы в очередь;
- fallback-сверка таблицы выполняется раз в минуту после попытки выгрузить ожидающие локальные изменения;
- `verify:core` проверяет уникальные message ID, stale callback, массовый день, возврат, сохранение и повторное открытие.
- обзор недели содержит `Выбрать все слоты`/`Снять все слоты`; callback одного чата сериализуются и защищены от повторной доставки;
- если Telegram не может отредактировать панель, бот отправляет одну восстановленную панель, удаляет старую и продолжает с тем же персистентным черновиком;
- transient-состояние чатовых панелей хранится отдельно и не является источником бизнес-данных.

Не подтверждено автоматически с локальной машины: production `sheets:roundtrip-test`, поскольку Google-ключ хранится только на Selectel. Его следует запускать после каждого изменения интеграции.
